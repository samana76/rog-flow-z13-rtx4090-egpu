# GPD controlled attach: ordinary automatic loading stays blocked.
# Only the manual single-attempt runner may bypass this gate.
blacklist nvidia
install nvidia /usr/bin/false
blacklist nvidia_uvm
install nvidia_uvm /usr/bin/false
blacklist nvidia_modeset
install nvidia_modeset /usr/bin/false
blacklist nvidia_drm
install nvidia_drm /usr/bin/false
blacklist nvidia_peermem
install nvidia_peermem /usr/bin/false
blacklist nouveau
install nouveau /usr/bin/false
