#!/usr/bin/python3
import fcntl,hashlib,json,os,stat,subprocess,sys
from pathlib import Path
BACK=Path("/root/gpd-egpu-dual-kernel-backup")
SOURCE=Path("/opt/gpd-egpu/candidates/615.71.09/7.2.5-1-cachyos")
FILES={'/opt/gpd-egpu/615.71.09/loader.py': {'before': '82127de15b2905f760dd7dea46b7dc1c8313a8c4c9342b9b4ef4cd6f31bb77f1', 'after': '1258e233f5e109b811f340c35ec9dc1c2900a6c1f46cf0e3c1e731008d582404', 'content': '#!/usr/bin/env python3\n"""Manual compute-only loader. One initialization attempt per boot; no recovery."""\nimport fcntl\nimport hashlib\nimport importlib.util\nimport json\nimport os\nfrom pathlib import Path\nimport re\nimport sys\nimport time\n\nKERNEL = os.uname().release\nBUILDS = {\n    \'7.2.5-1-cachyos\': (\'7ed66460ba04c99f45113d0e7de293810594aeb4fc70ab19bc834dc158344121\', \'9f7c3c79b3ed9438164849ba39df728e5105b9907883ecb4125377a3df3d5e0f\'),\n    \'7.2.4-1-cachyos\': (\'f1a9c9e550d56242741d73503c1b996d4642725c0a2ef9fcca345f90c8c774e3\', \'4b9ab4a30c9cd1b9aa9f7a8994bed769a269539b647b5ae19af42aa62cf5d3a1\'),\n    \'7.2.4-arch1-2\': (\'5bc38891199988d253af1112c4e104a44bf97dd07c24fb316d550b70c4b12869\', \'1afb65e395d1a6eb2aa35f1961f86861e954be7b617784d19a77167824b7d135\'),\n}\nROOT = Path(\'/opt/gpd-egpu/615.71.09\')/KERNEL\nSTATE = Path(\'/var/lib/gpd-egpu-loader\')\nCORE, UVM = BUILDS.get(KERNEL, (\'unsupported\', \'unsupported\'))\nGUARD = \'02ca6d1cd28371c0e05335e102476b406843dc1639731b01f6d3e19f801ab497\'\n\ndef startup_action(record, core, uvm):\n    if record is not None and record.get(\'stage\') != \'completed\':\n        raise RuntimeError(\'A loader attempt already failed or was interrupted this boot; no retry\')\n    if core and uvm:\n        return \'verify\'\n    if core or uvm or record is not None:\n        raise RuntimeError(\'Partial or changed driver state; no reload/recovery\')\n    return \'load\'\n\ndef guard():\n    if KERNEL not in BUILDS:\n        raise RuntimeError(\'Untested kernel; matching private modules must be built and validated first\')\n    p = ROOT.parent/\'guard.py\'\n    if hashlib.sha256(p.read_bytes()).hexdigest() != GUARD:\n        raise RuntimeError(\'Guard library checksum changed\')\n    spec = importlib.util.spec_from_file_location(\'gpd_guard\', p)\n    m = importlib.util.module_from_spec(spec)\n    spec.loader.exec_module(m)\n    return m\n\ndef boot_check(t, m):\n    entries = m.menu_entries(Path(\'/boot/limine.conf\').read_text())\n    for title in (\'//linux\', \'//linux-cachyos\'):\n        selected = [e for e in entries if e[\'title\'] == title]\n        m.require(len(selected) == 1, \'Missing/ambiguous normal boot entry: \'+title)\n        for key in (\'path\', \'module_path\'):\n            path, expected = m.boot_resource(selected[0][key])\n            m.require(m.digest(path, \'blake2b\') == expected, \'Boot checksum mismatch: \'+str(path))\n    t.log(\'Normal Arch fallback and CachyOS boot resource checksums match; no boot files changed\')\n\ndef required_packages(kernel):\n    # Validate only the package supplying the running supported kernel.\n    # Updating a different installed kernel must not disable Arch eGPU startup.\n    kernel_packages = {\n        \'7.2.5-1-cachyos\': (\'linux-cachyos\',\'7.2.5-1\'),\n        \'7.2.4-arch1-2\': (\'linux\', \'7.2.4.arch1-2\'),\n        \'7.2.4-1-cachyos\': (\'linux-cachyos\', \'7.2.4-1\'),\n    }\n    if kernel not in kernel_packages:\n        raise RuntimeError(\'Unsupported running kernel; build and validate its modules first\')\n    return [(\'nvidia-utils\', \'615.71.09-1\'), kernel_packages[kernel]]\n\ndef identity(t, m):\n    m.require(os.uname().release in BUILDS, \'Unsupported kernel\')\n    m.require(\'GZ302EA\' in t.read(\'/sys/class/dmi/id/product_name\'), \'Wrong laptop\')\n    m.require(t.read(\'/sys/class/dmi/id/bios_version\') == \'GZ302EA.314\', \'BIOS changed; review compatibility\')\n    cmd = t.read(\'/proc/cmdline\')\n    m.require(\'amdgpu.dcdebugmask=0x40600\' in cmd.split(), \'AMD command line changed\')\n    m.require(not any(s in cmd for s in (\'pcie_aspm=off\',\'pcie_ports=native\',\'pci=realloc\',\'thunderbolt.host_reset=0\',\'thunderbolt.host_reset=false\')), \'Unexpected PCI/USB4 command line\')\n    for name, version in required_packages(os.uname().release):\n        m.require(t.run([\'pacman\',\'-Q\',name]).strip() == name+\' \'+version, \'Kernel/driver package changed; private modules need review/rebuild\')\n    for name, expected in [(\'nvidia.ko\',CORE),(\'nvidia-uvm.ko\',UVM)]:\n        m.require(m.digest(ROOT/name) == expected, \'Module file changed: \'+name)\n        m.require(t.run([\'modinfo\',\'-F\',\'vermagic\',str(ROOT/name)]).split()[0] == KERNEL, \'Module kernel mismatch\')\n    m.validate_gate(m.GATE.read_text())\n    m.require(not any(Path(\'/sys/module\', x).exists() for x in (\'nvidia_drm\',\'nvidia_modeset\',\'nouveau\',\'nvidia_peermem\')), \'Unexpected display/other NVIDIA modules; no unbinding\')\n    m.require(t.run([\'systemctl\',\'is-enabled\',\'egpu-postboot-verify.service\'],check=False).strip() in (\'disabled\',\'masked\'), \'Old automatic verifier is enabled\')\n    m.require(t.run([\'systemctl\',\'is-active\',\'egpu-postboot-verify.service\'],check=False).strip() == \'inactive\', \'Old automatic verifier is active\')\n    boot_check(t,m)\n\ndef live(t, m):\n    m.require(t.read(\'/sys/module/nvidia/srcversion\') == \'329D97755EFA31D5314E8F0\', \'Loaded core differs from tested build\')\n    params = t.read(\'/proc/driver/nvidia/params\')\n    m.parse_dpm(params)\n    m.require(re.findall(r\'^EnableGpuFirmware:\\s*(\\S+)\\s*$\',params,re.M) == [\'0\'], \'GSP-disable option did not win\')\n\ndef find_gpu(t, m, wait):\n    end = time.monotonic()+(90 if wait else 0)\n    while True:\n        devices = t.devices()\n        m.require(len(devices) <= 1, \'Multiple NVIDIA display devices\')\n        if devices:\n            gpu = devices[0]\n            break\n        m.require(wait and time.monotonic() < end, \'GPU absent; connect/power enclosure before a future boot attempt; no rescan\')\n        time.sleep(min(1,max(0,end-time.monotonic())))\n    parent = Path(\'/sys/bus/pci/devices\')/gpu.resolve().parent.name\n    for p in (parent,gpu):\n        t.pci(p.name)\n    m.require(t.read(gpu/\'device\') == \'0x2684\', \'Not the tested RTX4090\')\n    m.require(t.read(parent/\'vendor\') == \'0x8086\' and t.read(parent/\'device\') == \'0x5786\', \'Not the tested JHL9480 parent\')\n    m.require(any(p in gpu.resolve().parts for p in m.PORTS), \'Unexpected USB4 topology\')\n    hda = [p for p in Path(\'/sys/bus/pci/devices\').glob(gpu.name.rsplit(\'.\',1)[0]+\'.*\') if (p/\'vendor\').read_text().strip() == \'0x10de\' and (p/\'class\').read_text().strip() == \'0x040300\']\n    t.state.update(gpu_bdf=gpu.name,parent_bdf=parent.name,realpath=str(gpu.resolve()))\n    return gpu,parent,hda\n\ndef nvml(t,m):\n    query = t.run([\'nvidia-smi\',\'-q\'])\n    m.require(re.findall(r\'^\\s*GSP Firmware Version\\s*:\\s*(.+)$\',query,re.M) == [\'N/A\'], \'GSP not confirmed disabled\')\n\ndef main():\n    if os.geteuid() != 0:\n        raise SystemExit(\'Run sudo gpd-egpu-start\')\n    if sys.argv[1:]:\n        raise SystemExit(\'No recovery, retry or automatic mode exists\')\n    os.umask(0o077)\n    lock = open(\'/run/lock/gpd-egpu-loader.lock\',\'a\')\n    fcntl.flock(lock,fcntl.LOCK_EX | fcntl.LOCK_NB)\n    m = guard()\n    boot = Path(\'/proc/sys/kernel/random/boot_id\').read_text().strip()\n    m.PRIVATE = STATE/boot\n    m.LOGDIR = Path(\'/var/log/egpu-autoconfig/loader\')/boot\n    t = m.Trial()\n    try:\n        identity(t,m)\n        existing = json.loads((m.PRIVATE/\'attempt.json\').read_text()) if m.PRIVATE.exists() else None\n        action = startup_action(existing,Path(\'/sys/module/nvidia\').exists(),Path(\'/sys/module/nvidia_uvm\').exists())\n        if action == \'load\':\n            m.PRIVATE.mkdir(parents=True,mode=0o700)\n            t.owned = True\n            import datetime\n            t.state[\'journal_since\'] = datetime.datetime.now().astimezone().strftime(\'%Y-%m-%d %H:%M:%S\')\n            t.checkpoint(\'manual-attempt-started\')\n            for b in m.PORTS:\n                t.pin(Path(\'/sys/bus/pci/devices\')/b)\n            t.log(\'Waiting up to90seconds for powered enclosure; display activation follows compute initialization\')\n            gpu,parent,hda = find_gpu(t,m,True)\n            m.require(not (gpu/\'driver\').exists(), \'GPU already bound\')\n            t.run([\'udevadm\',\'settle\',\'--timeout=10\'])\n            for p in (gpu,*hda):\n                t.pin(p)\n            t.start_follow()\n            t.checkpoint(\'driverless-immediate-check\')\n            t.check_link(gpu,parent,hda)\n            t.check_errors()\n            t.checkpoint(\'before-private-core-load\')\n            os.sync()\n            t.run([\'insmod\',str(ROOT/\'nvidia.ko\'),\'NVreg_DynamicPowerManagement=0\',\'NVreg_EnableGpuFirmware=0\'],timeout=45)\n            end = time.monotonic()+10\n            while not (gpu/\'driver\').exists() and time.monotonic() < end:\n                t.pci(parent.name); t.pci(gpu.name); time.sleep(.25)\n            m.require((gpu/\'driver\').exists() and (gpu/\'driver\').resolve().name == \'nvidia\', \'Core did not bind\')\n            t.run([\'udevadm\',\'settle\',\'--timeout=10\'])\n            live(t,m)\n            t.check_link(gpu,parent,hda)\n            t.checkpoint(\'before-first-nvml\')\n            os.sync()\n            nvml(t,m)\n            t.checkpoint(\'initialized-immediate-check\')\n            t.check_link(gpu,parent,hda)\n            t.check_errors()\n            t.checkpoint(\'before-private-uvm-load\')\n            os.sync()\n            t.run([\'insmod\',str(ROOT/\'nvidia-uvm.ko\')],timeout=30)\n        else:\n            live(t,m)\n            gpu,parent,hda = find_gpu(t,m,False)\n            if existing:\n                m.require(existing.get(\'realpath\') == str(gpu.resolve()), \'GPU topology changed since initialization; no recovery\')\n            t.log(\'Already loaded: validating existing driver without reloading\')\n        m.require(t.read(\'/sys/module/nvidia_uvm/srcversion\') == \'064C13389A454AA4AAF638E\', \'Loaded UVM differs from tested build\')\n        t.check_link(gpu,parent,hda)\n        live(t,m)\n        nvml(t,m)\n        if t.owned:\n            t.check_errors()\n            t.state[\'result\'] = \'READY: proprietary615 DPM0 GSP-off core/UVM compute path\'\n            t.checkpoint(\'completed\')\n        t.log(\'READY: RTX4090 compute driver loaded; GSP disabled, DPM0, power pins on. Desktop remains on AMD. No model workload tested here.\')\n    except (Exception,KeyboardInterrupt) as exc:\n        t.log(\'FAIL: \'+str(exc)+\'; no recovery, unload, reset or automatic retry\')\n        if t.owned:\n            t.state[\'result\'] = \'FAIL: \'+str(exc)\n            t.checkpoint(\'failed\')\n        raise SystemExit(1)\n    finally:\n        t.stop_follow()\n\nif __name__ == \'__main__\':\n    main()\n'}, '/opt/gpd-egpu/display-extension/display-start.py': {'before': 'ce4b95f8fd6b55ae154be9b32f82afd64550adb6d12a89b7d7162061865eea04', 'after': 'c6f6237b172b7f8ace66387b521b6bf913842d04d8c24bd89ac0150336fd06fd', 'content': '#!/usr/bin/env python3\n"""One volatile Arch display trial on the already validated compute driver."""\nimport datetime\nimport fcntl\nimport hashlib\nimport importlib.util\nimport json\nimport os\nfrom pathlib import Path\nimport sys\nimport time\n\nHERE = Path(__file__).resolve().parent\nSTATE = Path(\'/var/lib/gpd-egpu-display\')/Path(\'/proc/sys/kernel/random/boot_id\').read_text().strip()\nDISPLAY_BUILDS = {\n \'7.2.4-arch1-2\': (\'display-payload\', {\'nvidia-modeset.ko\':\'58db7a3b74657de9545f4ea7a8de99a35c2a3b4148e024e57c0b25ba848da003\',\'nvidia-drm.ko\':\'69c7e08562525d25578bf0020b85b5ca8aad3d26d7da8c1ab7c8ea831cb48630\'}),\n \'7.2.5-1-cachyos\': (\'display-payload-7.2.5-1-cachyos\', {\'nvidia-modeset.ko\': \'1d3694df2a7c16e19a83be9a8d3793727c712df15f1f07df102d5ba6002a95ea\', \'nvidia-drm.ko\': \'cb56a5bd3ea8ecf94bb3dfaad34623162f5362bd5243dbf7131a9596207efc8a\'})\n}\nPAYLOAD, FILES = DISPLAY_BUILDS.get(os.uname().release, (\'unsupported\', {}))\n\ndef load(path):\n    spec = importlib.util.spec_from_file_location(\'display_guard\', path)\n    m = importlib.util.module_from_spec(spec); spec.loader.exec_module(m); return m\n\ndef main():\n    if os.geteuid() != 0 or sys.argv[1:]:\n        raise SystemExit(\'Run manually with sudo, no arguments\')\n    os.umask(0o077)\n    # Compute loader takes the same lock itself. It only runs if display is absent.\n    if not Path(\'/sys/module/nvidia_modeset\').exists() and not Path(\'/sys/module/nvidia_drm\').exists():\n        import subprocess\n        subprocess.run([\'/usr/bin/python3\',\'/opt/gpd-egpu/615.71.09/loader.py\'],check=True,timeout=360)\n    lock = open(\'/run/lock/gpd-egpu-loader.lock\',\'a\')\n    fcntl.flock(lock,fcntl.LOCK_EX | fcntl.LOCK_NB)\n    source = Path(\'/opt/gpd-egpu/615.71.09/loader.py\')\n    if hashlib.sha256(source.read_bytes()).hexdigest() != \'1258e233f5e109b811f340c35ec9dc1c2900a6c1f46cf0e3c1e731008d582404\':\n        raise SystemExit(\'Installed compute loader changed; stop for review\')\n    l = load(source); m = l.guard()\n    m.PRIVATE = STATE\n    m.LOGDIR = Path(\'/var/log/egpu-autoconfig/display-loader\')/STATE.name\n    t = m.Trial()\n    try:\n        m.require(os.uname().release in DISPLAY_BUILDS, \'Unsupported display kernel\')\n        previous = json.loads((STATE/\'attempt.json\').read_text()) if STATE.exists() else None\n        m.require(previous is None or previous.get(\'stage\') == \'completed\', \'Display attempt failed/interrupted this boot; no retry\')\n        modeset = Path(\'/sys/module/nvidia_modeset\').exists()\n        drm = Path(\'/sys/module/nvidia_drm\').exists()\n        m.require(modeset == drm, \'Partial display state; no reload\')\n        if modeset:\n            l.boot_check(t,m); l.live(t,m)\n            m.validate_gate(m.GATE.read_text())\n            gpu,parent,hda = l.find_gpu(t,m,False)\n            t.check_link(gpu,parent,hda)\n            for name,expected in [(\'nvidia_modeset\',\'C86CA92D8E48780F19CB46E\'),(\'nvidia_drm\',\'E22EBDCD6BCC5889463CFBF\')]:\n                m.require(t.read(\'/sys/module/\'+name+\'/srcversion\') == expected, \'Loaded display build changed\')\n            m.require(t.read(\'/sys/module/nvidia_drm/parameters/modeset\') == \'Y\', \'Modesetting disabled\')\n            m.require(t.read(\'/sys/module/nvidia_drm/parameters/fbdev\') == \'N\', \'Framebuffer policy changed\')\n            l.nvml(t,m)\n            t.log(\'READY: existing Arch compute and display modules verified; no reload\')\n            return\n        m.require(previous is None, \'Completed display modules disappeared; no reload\')\n        l.identity(t,m); l.live(t,m)\n        m.require(t.read(\'/sys/module/nvidia_uvm/srcversion\') == \'064C13389A454AA4AAF638E\', \'Expected UVM missing\')\n        gpu,parent,hda = l.find_gpu(t,m,False)\n        t.check_link(gpu,parent,hda); l.nvml(t,m)\n        for dep in (\'video\',\'drm_display_helper\',\'drm_ttm_helper\'):\n            m.require(Path(\'/sys/module\',dep).exists(), \'Shared dependency not loaded: \'+dep)\n        for name,expected in FILES.items():\n            p = HERE/PAYLOAD/name\n            m.require(m.digest(p) == expected, \'Module hash mismatch: \'+name)\n            m.require(t.run([\'modinfo\',\'-F\',\'vermagic\',str(p)]).split()[0] == l.KERNEL, \'Module kernel mismatch\')\n            m.require(t.run([\'modinfo\',\'-F\',\'version\',str(p)]).strip() == \'615.71.09\', \'Module version mismatch\')\n        STATE.mkdir(parents=True,mode=0o700)\n        t.owned = True\n        for name in FILES:\n            (STATE/name).write_bytes((HERE/PAYLOAD/name).read_bytes())\n            m.require(m.digest(STATE/name) == FILES[name], \'Private copy mismatch\')\n        (STATE/\'runner.py\').write_bytes(Path(__file__).read_bytes())\n        # Plasma can save output layout on DRM hotplug. Preserve the pre-trial file.\n        config = Path(\'/home/EGPU_USER/.config/kwinoutputconfig.json\')\n        t.state[\'plasma_output_config_existed\'] = config.exists()\n        if config.exists(): (STATE/\'kwinoutputconfig.json.before\').write_bytes(config.read_bytes())\n        t.state[\'journal_since\'] = datetime.datetime.now().astimezone().strftime(\'%Y-%m-%d %H:%M:%S\')\n        t.state[\'undo\'] = \'Normal reboot unloads these volatile display modules; automatic gate stays unchanged. Saved Plasma layout is retained for review, never restored over a running compositor.\'\n        t.checkpoint(\'display-baseline-ready\')\n        t.start_follow()\n        for name,args in [(\'nvidia-modeset.ko\',[]),(\'nvidia-drm.ko\',[\'modeset=1\',\'fbdev=0\'])]:\n            t.check_link(gpu,parent,hda)\n            t.checkpoint(\'before-\'+name)\n            os.sync()\n            t.run([\'insmod\',str(STATE/name),*args],timeout=45)\n            t.check_link(gpu,parent,hda); t.check_errors(); l.live(t,m)\n        m.require(t.read(\'/sys/module/nvidia_drm/parameters/modeset\') == \'Y\', \'DRM modeset not enabled\')\n        m.require(t.read(\'/sys/module/nvidia_drm/parameters/fbdev\') == \'N\', \'Unexpected framebuffer takeover\')\n        t.checkpoint(\'display-immediate-check\')\n        t.check_link(gpu,parent,hda); t.check_errors(); l.live(t,m); l.nvml(t,m)\n        connectors = {}\n        for card in Path(\'/sys/class/drm\').glob(\'card[0-9]*\'):\n            if \'-\' in card.name or not (card/\'device\').exists() or (card/\'device\').resolve() != gpu.resolve(): continue\n            for p in Path(\'/sys/class/drm\').glob(card.name+\'-*\'):\n                if (p/\'status\').exists():\n                    connectors[p.name] = {key:t.read(p/key) for key in (\'status\',\'enabled\',\'modes\') if (p/key).exists()}\n        t.state[\'connectors\'] = connectors\n        m.require(connectors, \'NVIDIA DRM exposes no connectors\')\n        t.result(\'PASS: repeatable NVIDIA display modules loaded with modeset=1/fbdev=0; PCI/NVML healthy at immediate check. Connector evidence recorded; visible monitor output still requires confirmation.\')\n    except (Exception,KeyboardInterrupt) as e:\n        t.result(\'FAIL: display trial: \'+str(e)+\'; no unload/recovery/retry\')\n        raise SystemExit(1)\n    finally:\n        t.stop_follow()\n\nif __name__ == \'__main__\': main()\n'}, '/usr/local/sbin/gpd-egpu-start': {'before': 'a980026e599653b390bcea1782be008dd7de782e3173a75405f2e2a346976774', 'after': '68c09c249104918c84ed27b7f5514f02c22737d0381ce54f28208bfd079e0245', 'content': '#!/bin/bash\nset -euo pipefail\ncase "$(uname -r)" in\n  7.2.4-arch1-2|7.2.5-1-cachyos) exec /usr/bin/python3 /opt/gpd-egpu/display-extension/display-start.py "$@" ;;\n  *) exec /usr/bin/python3 /opt/gpd-egpu/615.71.09/loader.py "$@" ;;\nesac\n'}, '/opt/gpd-egpu/auto/restart-clean-cycle.py': {'before': 'b09c0907c13d689a20db7d798c2fc2f4a36ba4d5f8e9b75bf43db90359709bbf', 'after': 'e728f950fa4d0fced0bdd02e0a416e9b2fac3e4b3a1fcee01f44e91669c15aec', 'content': '#!/usr/bin/env python3\n"""One restart per recorded clean-eject cycle. No driver replacement."""\nimport datetime\nimport fcntl\nimport hashlib\nimport importlib.util\nimport json\nimport os\nfrom pathlib import Path\nimport subprocess\nimport sys\nimport time\n\nsys.dont_write_bytecode=True\nEXPECTED={\n \'/opt/gpd-egpu/615.71.09/loader.py\':\'1258e233f5e109b811f340c35ec9dc1c2900a6c1f46cf0e3c1e731008d582404\',\n \'/opt/gpd-egpu/display-extension/display-start.py\':\'c6f6237b172b7f8ace66387b521b6bf913842d04d8c24bd89ac0150336fd06fd\',\n \'/usr/local/sbin/gpd-egpu-start\':\'68c09c249104918c84ed27b7f5514f02c22737d0381ce54f28208bfd079e0245\',\n \'/usr/local/libexec/gpd-egpu-eject\':\'5f6bfc8d267f6818a81c5834534164c069e33bb3c3156c6415316caba1984fde\',\n}\n\ndef check(value,message):\n    if not value:raise RuntimeError(message)\n\ndef validate_records(boot,eject,core,display,loaded):\n    check(not loaded,\'NVIDIA modules remain loaded; no unload/reload attempted\')\n    check(eject.get(\'boot\')==boot and eject.get(\'stage\')==\'all-nvidia-modules-unloaded\',\n          \'No completed clean eject for this boot; no retry\')\n    for label,record in [(\'core\',core),(\'display\',display)]:\n        check(record.get(\'boot_id\')==boot and record.get(\'stage\')==\'completed\',\n              label+\' loader did not previously complete this boot; no retry\')\n\ndef archive(source,dest):\n    check(source.is_dir() and not source.is_symlink(),\'Unexpected record directory: \'+str(source))\n    check(not dest.exists(),\'Archive destination already exists\')\n    source.rename(dest)\n    for p in (source.parent,dest.parent):\n        fd=os.open(p,os.O_DIRECTORY)\n        try:os.fsync(fd)\n        finally:os.close(fd)\n\ndef cycle_id(eject):\n    return hashlib.sha256((eject/\'state.json\').read_bytes()+b\'\\0\'+(eject/\'operations.log\').read_bytes()).hexdigest()\n\ndef validate_marker(automatic, marker, boot, cycle):\n    if automatic:\n        check(marker is not None and marker.get(\'boot\')==boot and marker.get(\'cycle\')==cycle and marker.get(\'action\')==\'clean-eject-restart\',\'Automatic invocation does not match its checkpoint\')\n    else:\n        check(marker is None,\'Automatic loader failure marker exists; no retry\')\n\ndef main():\n    check(os.geteuid()==0 and sys.argv[1:] in ([],[\'--automatic\']),\'Root execution required; invalid arguments\')\n    automatic=sys.argv[1:]==[\'--automatic\']\n    os.umask(0o077)\n    boot=Path(\'/proc/sys/kernel/random/boot_id\').read_text().strip()\n    eject_source=Path(\'/var/lib/gpd-egpu-eject\')/boot\n    cycle=cycle_id(eject_source)\n    base=Path(\'/var/lib/gpd-egpu-restart\')/boot/cycle\n    check(not base.exists(),\'Restart already attempted for this eject cycle; inspect its log, no automatic retry\')\n    lock=open(\'/run/lock/gpd-egpu-loader.lock\',\'a\')\n    fcntl.flock(lock,fcntl.LOCK_EX|fcntl.LOCK_NB)\n    for name,digest in EXPECTED.items():\n        check(hashlib.sha256(Path(name).read_bytes()).hexdigest()==digest,\'Working driver helper changed: \'+name)\n    spec=importlib.util.spec_from_file_location(\'working_loader\',next(iter(EXPECTED)))\n    l=importlib.util.module_from_spec(spec);spec.loader.exec_module(l)\n    m=l.guard();m.LOGDIR=Path(\'/var/log/egpu-autoconfig/restart-after-eject\')/boot\n    t=m.Trial()\n    try:\n        t.log(\'SCRIPT SHA256 \'+hashlib.sha256(Path(__file__).read_bytes()).hexdigest())\n        check(os.uname().release in (\'7.2.4-arch1-2\',\'7.2.5-1-cachyos\'),\'Unsupported restart kernel\')\n        core=Path(\'/var/lib/gpd-egpu-loader\')/boot\n        display=Path(\'/var/lib/gpd-egpu-display\')/boot\n        eject=Path(\'/var/lib/gpd-egpu-eject\')/boot\n        records=[json.loads((p/f).read_text()) for p,f in [(eject,\'state.json\'),(core,\'attempt.json\'),(display,\'attempt.json\')]]\n        validate_records(boot,*records,any(Path(\'/sys/module\',n).exists() for n in (\'nvidia\',\'nvidia_uvm\',\'nvidia_modeset\',\'nvidia_drm\',\'nouveau\',\'nvidia_peermem\')))\n        l.identity(t,m)\n        marker=Path(\'/var/lib/gpd-egpu-auto/initializing-or-failed.json\')\n        validate_marker(automatic,json.loads(marker.read_text()) if marker.exists() else None,boot,cycle)\n        check((Path(\'/var/lib/gpd-egpu-auto\')/(boot+\'.completed\')).is_file(),\n              \'Automatic first-activation completion record missing; review concurrency before restarting\')\n        check(t.run([\'systemctl\',\'is-active\',\'gpd-egpu-auto.service\'],check=False).strip()in ((\'activating\',) if automatic else (\'inactive\',)),\'Unexpected automatic loader state\')\n        check(not Path(\'/run/gpd-egpu-display-trial.active\').exists(),\'Experimental candidate trial remains active\')\n        gpu,parent,hda=l.find_gpu(t,m,False)\n        check(not (gpu/\'driver\').exists(),\'GPU is already bound; no takeover\')\n        for p in (parent,gpu,*hda):t.pci(p.name)\n        panels=[p for p in Path(\'/sys/class/drm\').glob(\'card*-eDP-*\')\n                if (p/\'enabled\').read_text().strip()==\'enabled\'\n                and (p.parent/p.name.split(\'-\')[0]/\'device/vendor\').read_text().strip()==\'0x1002\']\n        check(panels,\'Internal AMD panel must be enabled\')\n        base.mkdir(parents=True,mode=0o700)\n        record={\'boot\':boot,\'stage\':\'prepared\',\'gpu\':gpu.name,\'parent\':parent.name,\n                \'undo\':\'No driver/configuration/package/boot changes. Normal reboot clears restarted modules and uses normal automatic loading. Original and new per-boot histories are retained.\',\n                \'source_hashes\':EXPECTED,\'archived\':[]}\n        def checkpoint(stage):\n            record[\'stage\']=stage\n            tmp=base/\'state.json.tmp\'\n            with tmp.open(\'w\') as f:json.dump(record,f,indent=2);f.flush();os.fsync(f.fileno())\n            tmp.replace(base/\'state.json\');os.sync();t.log(\'CHECKPOINT \'+stage)\n        (base/\'runner.py\').write_bytes(Path(__file__).read_bytes())\n        config=Path(\'/home/EGPU_USER/.config/kwinoutputconfig.json\')\n        if config.exists():(base/\'kwinoutputconfig.before\').write_bytes(config.read_bytes())\n        checkpoint(\'before-archive\')\n        for label,p in [(\'core\',core),(\'display\',display)]:\n            record[\'archive_intent\']=[str(p),str(base/(label+\'-before\'))];checkpoint(\'archiving-\'+label)\n            archive(p,base/(label+\'-before\'));record[\'archived\'].append(label)\n        checkpoint(\'before-working-loader\')\n        # The unchanged auto completion marker prevents udev from restarting it.\n        # Release the shared lock for the existing loader, which acquires it.\n        fcntl.flock(lock,fcntl.LOCK_UN)\n        try:\n            t.run([\'/usr/local/sbin/gpd-egpu-start\'],timeout=540)\n        except BaseException:\n            checkpoint(\'failed-no-retry\');raise\n        fcntl.flock(lock,fcntl.LOCK_EX|fcntl.LOCK_NB)\n        l.live(t,m)\n        for module,expected in [(\'nvidia_modeset\',\'C86CA92D8E48780F19CB46E\'),(\'nvidia_drm\',\'E22EBDCD6BCC5889463CFBF\')]:\n            check(t.read(\'/sys/module/\'+module+\'/srcversion\')==expected,\'Unexpected restarted display module\')\n        gpu,parent,hda=l.find_gpu(t,m,False);t.check_link(gpu,parent,hda);l.nvml(t,m)\n        for p in (core,display):check(json.loads((p/\'attempt.json\').read_text())[\'stage\']==\'completed\',\'New loader record incomplete\')\n        check(json.loads((eject/\'state.json\').read_text())==records[0],\'Eject record changed concurrently\')\n        checkpoint(\'before-retiring-completed-eject\')\n        archive(eject,base/\'eject-before\')\n        checkpoint(\'completed\')\n        t.log(\'READY: original NVIDIA compute/display drivers restarted after clean eject. Disconnect app is available again. Keep cable connected until the app reports READY TO UNPLUG. Surprise unplug remains unsafe. LACT state unchanged.\')\n    except (Exception,KeyboardInterrupt) as e:\n        t.log(\'FAIL: \'+str(e)+\'; no unload, recovery, record deletion or retry attempted. Evidence: \'+str(base))\n        raise SystemExit(1)\n    finally:t.stop_follow()\n\nif __name__==\'__main__\':main()\n'}, '/opt/gpd-egpu/auto/auto-start.py': {'before': '2328b799bba9253aa64b3d5c1b925816fee9e6ccc4c1d1e4f117dfdd4a046b66', 'after': '800c1c117ab634ad560022c5d54c53c9d66f6334536dc9b6853609b2a548b2fc', 'content': '#!/usr/bin/env python3\n"""Original first activation plus restart following a recorded clean eject."""\nimport importlib.util\nimport json\nimport os\nfrom pathlib import Path\nimport subprocess\nimport sys\nimport time\n\nsys.dont_write_bytecode=True\nSTATE=Path(\'/var/lib/gpd-egpu-auto\')\n\ndef action(completed,loaded,eject):\n    if not completed:return \'first\'\n    if loaded:return \'skip\'\n    if eject and eject.get(\'stage\')==\'all-nvidia-modules-unloaded\':return \'restart\'\n    return \'skip\'\n\ndef main():\n    if os.uname().release not in (\'7.2.4-arch1-2\',\'7.2.5-1-cachyos\'):return\n    gpus=[]\n    for p in Path(\'/sys/bus/pci/devices\').iterdir():\n        try:\n            if (p/\'vendor\').read_text().strip()==\'0x10de\' and (p/\'class\').read_text().strip().startswith(\'0x03\'):gpus.append(p.name)\n        except FileNotFoundError:continue\n    if not gpus:return\n    STATE.mkdir(parents=True,exist_ok=True)\n    marker=STATE/\'initializing-or-failed.json\'\n    if marker.exists():raise SystemExit(\'Failure/interruption lockout exists; no automatic retry\')\n    boot=Path(\'/proc/sys/kernel/random/boot_id\').read_text().strip()\n    completed=STATE/(boot+\'.completed\')\n    directory=Path(\'/var/lib/gpd-egpu-eject\')/boot\n    eject=json.loads((directory/\'state.json\').read_text()) if (directory/\'state.json\').exists() else None\n    selected=action(completed.exists(),any(Path(\'/sys/module\',n).exists() for n in (\'nvidia\',\'nvidia_uvm\',\'nvidia_drm\',\'nvidia_modeset\')),eject)\n    if selected==\'skip\':\n        print(\'No clean-eject restart required or authorized\');return\n    record={\'boot\':boot,\'time\':time.time(),\'gpu\':gpus,\'action\':\'first\'}\n    command=[\'/usr/local/sbin/gpd-egpu-start\']\n    if selected==\'restart\':\n        helper=Path(\'/opt/gpd-egpu/auto/restart-clean-cycle.py\')\n        spec=importlib.util.spec_from_file_location(\'clean_cycle\',helper)\n        m=importlib.util.module_from_spec(spec);spec.loader.exec_module(m)\n        record.update(action=\'clean-eject-restart\',cycle=m.cycle_id(directory))\n        command=[\'/usr/bin/python3\',str(helper),\'--automatic\']\n    with marker.open(\'x\') as f:\n        json.dump(record,f);f.flush();os.fsync(f.fileno())\n    os.sync()\n    subprocess.run(command,check=True,timeout=540)\n    completed.write_text(\'Activation completed. Use disconnect app before physical unplug.\\n\')\n    os.sync();marker.unlink();os.sync()\n    print(\'Automatic \'+selected+\' completed; surprise unplug remains unsafe\')\n\nif __name__==\'__main__\':main()\n'}}
MODULES={'nvidia.ko': {'sha256': '7ed66460ba04c99f45113d0e7de293810594aeb4fc70ab19bc834dc158344121', 'version': '615.71.09', 'vermagic': '7.2.5-1-cachyos SMP preempt mod_unload', 'license': 'NVIDIA', 'srcversion': '329D97755EFA31D5314E8F0', 'dest': '/opt/gpd-egpu/615.71.09/7.2.5-1-cachyos/nvidia.ko'}, 'nvidia-uvm.ko': {'sha256': '9f7c3c79b3ed9438164849ba39df728e5105b9907883ecb4125377a3df3d5e0f', 'version': '615.71.09', 'vermagic': '7.2.5-1-cachyos SMP preempt mod_unload', 'license': 'Dual MIT/GPL', 'srcversion': '064C13389A454AA4AAF638E', 'dest': '/opt/gpd-egpu/615.71.09/7.2.5-1-cachyos/nvidia-uvm.ko'}, 'nvidia-modeset.ko': {'sha256': '1d3694df2a7c16e19a83be9a8d3793727c712df15f1f07df102d5ba6002a95ea', 'version': '615.71.09', 'vermagic': '7.2.5-1-cachyos SMP preempt mod_unload', 'license': 'NVIDIA', 'srcversion': 'C86CA92D8E48780F19CB46E', 'dest': '/opt/gpd-egpu/display-extension/display-payload-7.2.5-1-cachyos/nvidia-modeset.ko'}, 'nvidia-drm.ko': {'sha256': 'cb56a5bd3ea8ecf94bb3dfaad34623162f5362bd5243dbf7131a9596207efc8a', 'version': '615.71.09', 'vermagic': '7.2.5-1-cachyos SMP preempt mod_unload', 'license': 'MIT', 'srcversion': 'E22EBDCD6BCC5889463CFBF', 'dest': '/opt/gpd-egpu/display-extension/display-payload-7.2.5-1-cachyos/nvidia-drm.ko'}}
def check(ok,msg):
 if not ok:raise RuntimeError(msg)
def sha(p):return hashlib.sha256(p.read_bytes()).hexdigest()
def put(p,data,mode):
 tmp=p.with_name(p.name+'.dual-kernel-tmp')
 with tmp.open('xb') as f:f.write(data);f.flush();os.fsync(f.fileno())
 tmp.chmod(mode);tmp.replace(p)
def unloaded():
 check(not any(Path('/sys/module',n).exists() for n in ('nvidia','nvidia_uvm','nvidia_modeset','nvidia_drm','nouveau')),'NVIDIA must be unloaded; use safe eject first')
 check(not any((p/'vendor').read_text().strip()=='0x10de' and (p/'class').read_text().strip().startswith('0x03') for p in Path('/sys/bus/pci/devices').iterdir()),'Physically disconnect after safe eject before installation/undo')
def undo():
 unloaded();r=json.loads((BACK/'manifest.json').read_text())
 for name,v in r['modified'].items():
  p=Path(name);check(not p.is_symlink() and sha(p) in (v['before'],v['after']),'Modified production file: '+name)
  check(sha(BACK/v['backup'])==v['before'],'Corrupt backup')
 for name,h in r['created'].items():
  p=Path(name);check(not p.is_symlink() and (not p.exists() or sha(p)==h),'Created module changed: '+name)
 # Do not revive old eject state if a subsequent connection generated new records.
 for name,v in r['modified'].items():put(Path(name),(BACK/v['backup']).read_bytes(),v['mode'])
 for name in r['created']:
  p=Path(name)
  if p.exists():p.unlink()
 for name in r['directories']:
  p=Path(name)
  if p.exists():
   try:p.rmdir()
   except OSError:pass
 os.sync();print('Automatic CachyOS support undone. Archived eject history retained; no old per-boot state replayed. No boot/package/service changes.')
def validate_partial_backup(base,files,modules,eject):
 check(not (base/'installation-completed.txt').exists(),'Installation already completed')
 r=json.loads((base/'manifest.json').read_text())
 check(set(r['modified'])==set(files),'Unexpected backup file set')
 check(r['created']=={v['dest']:v['sha256'] for v in modules.values()},'Unexpected module ownership record')
 check(r['eject_archive']==eject,'Backup is from a different boot/eject')
 for name,v in files.items():
  saved=r['modified'][name]
  check(saved['before']==v['before'] and saved['after']==v['after'],'Backup identity differs')
  check(Path(saved['backup']).name==saved['backup'],'Invalid backup path')
  check(sha(base/saved['backup'])==v['before'],'Original backup damaged')
  check(not Path(name).is_symlink() and sha(Path(name))==v['before'],'Previous installer did not restore original helper: '+name)
 return r

def archive_eject(source,base):
 # A sibling archive stays on the source filesystem, including separate Btrfs subvolumes.
 target=source.with_name('.trial-history-'+source.name)
 check(source.is_dir() and not source.is_symlink(),'Unexpected eject directory')
 check(not target.exists(),'Eject archive already exists; review before continuing')
 (base/'archive-location.json').write_text(json.dumps({'source':str(source),'archive':str(target)}))
 os.sync()
 source.rename(target)
 fd=os.open(source.parent,os.O_DIRECTORY)
 try:os.fsync(fd)
 finally:os.close(fd)

def main():
 check(os.geteuid()==0 and sys.argv[1:] in ([],['--undo']),'Run with sudo, optional --undo only');os.umask(0o077)
 lock=open('/run/lock/gpd-egpu-loader.lock','a');fcntl.flock(lock,fcntl.LOCK_EX|fcntl.LOCK_NB)
 check(subprocess.run(['systemctl','is-active','gpd-egpu-auto.service'],capture_output=True,text=True).stdout.strip()=='inactive','Automatic loader must be idle')
 if sys.argv[1:]:undo();return
 unloaded();check(os.uname().release=='7.2.5-1-cachyos','Install from tested CachyOS kernel');resume=BACK.exists()
 boot=Path('/proc/sys/kernel/random/boot_id').read_text().strip();eject=Path('/var/lib/gpd-egpu-eject')/boot
 state=json.loads((eject/'state.json').read_text());check(state.get('boot')==boot and state.get('stage')=='all-nvidia-modules-unloaded','Completed clean eject required')
 trial=json.loads(Path('/root/egpu-cachyos-7.2.5-validation/attempt.json').read_text());check(trial.get('boot_id')==boot and trial.get('stage')=='completed' and trial.get('result','').startswith('PASS:'),'Completed trial required')
 check(not Path('/var/lib/gpd-egpu-auto/initializing-or-failed.json').exists(),'Unresolved loader failure')
 check(not (Path('/var/lib/gpd-egpu-auto')/(boot+'.completed')).exists(),'Unexpected prior production activation this boot')
 for name,r in FILES.items():
  p=Path(name);check(not p.is_symlink() and sha(p)==r['before'],'Production helper changed: '+name)
  check(hashlib.sha256(r['content'].encode()).hexdigest()==r['after'],'Payload hash mismatch')
  if name.endswith('.py'):compile(r['content'],name,'exec')
  else:subprocess.run(['bash','-n'],input=r['content'],text=True,check=True)
 created={};directories=[]
 for name,r in MODULES.items():
  src=SOURCE/name;dst=Path(r['dest']);check(not dst.is_symlink() and (not dst.exists() or (resume and sha(dst)==r['sha256'])),'Unexpected existing destination')
  check(sha(src)==r['sha256'],'Candidate module changed')
  check(subprocess.check_output(['modinfo','-F','vermagic',str(src)],text=True).split()[0]=='7.2.5-1-cachyos','Wrong module kernel')
  created[str(dst)]=r['sha256']
  if not dst.parent.exists() and str(dst.parent) not in directories:directories.append(str(dst.parent))
 if resume:
  manifest=validate_partial_backup(BACK,FILES,MODULES,str(eject))
  modified=manifest['modified']
  (BACK/'resume-installer.py').write_bytes(Path(__file__).read_bytes())
 else:
  BACK.mkdir(mode=0o700);modified={}
  for i,(name,r) in enumerate(FILES.items()):
   p=Path(name);saved=str(i)+'.before';(BACK/saved).write_bytes(p.read_bytes());modified[name]={'before':r['before'],'after':r['after'],'mode':stat.S_IMODE(p.stat().st_mode),'backup':saved}
  manifest={'modified':modified,'created':created,'directories':directories,'eject_archive':str(eject)}
  (BACK/'manifest.json').write_text(json.dumps(manifest,indent=2));(BACK/'installer.py').write_bytes(Path(__file__).read_bytes())
  (BACK/'rollback.sh').write_text('#!/bin/bash\nset -euo pipefail\nexec /usr/bin/python3 /root/gpd-egpu-dual-kernel-backup/installer.py --undo\n');(BACK/'rollback.sh').chmod(0o700)
  subprocess.run(['bash','-n',str(BACK/'rollback.sh')],check=True);os.sync()

 try:
  for name,r in MODULES.items():
   dst=Path(r['dest']);dst.parent.mkdir(parents=True,exist_ok=True);put(dst,(SOURCE/name).read_bytes(),0o644);check(sha(dst)==r['sha256'],'Module copy mismatch')
  for name,r in FILES.items():put(Path(name),r['content'].encode(),modified[name]['mode'])
  for name,r in FILES.items():check(sha(Path(name))==r['after'],'Production write mismatch')
  # Preserve the trial eject rather than allowing it to block the next production eject.
  archive_eject(eject, BACK)
 except BaseException:
  for name,r in modified.items():put(Path(name),(BACK/r['backup']).read_bytes(),r['mode'])
  raise
 (BACK/'installation-completed.txt').write_text('Completed with same-filesystem eject archive\n');os.sync();print('INSTALLED: Arch and CachyOS7.2.5 automatic compute/display loading and clean-eject reconnect support. No driver load or service restart performed. Reconnect the eGPU now to test automatic activation. Rollback this layer before earlier rollbacks: '+str(BACK/'rollback.sh'))
if __name__=='__main__':
 try:main()
 except Exception as e:raise SystemExit('STOP: '+str(e)+'; no recovery or reboot')
