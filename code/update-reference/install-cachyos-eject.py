#!/usr/bin/python3
"""Add controlled CachyOS eject; do not load/unload or arm new-kernel startup."""
import fcntl,hashlib,json,os,stat,subprocess,sys
from pathlib import Path
BACK=Path('/root/gpd-egpu-cachyos-7.2.5-eject-backup')
FILES={'/usr/local/libexec/gpd-egpu-eject': {'before': '0e3a3bbe01e141d3b9014e11d93b889df0f2a2cc1885158dee738973c5c168b6', 'after': '5f6bfc8d267f6818a81c5834534164c069e33bb3c3156c6415316caba1984fde', 'content': '#!/usr/bin/env python3\n"""Experimental controlled eject: release DRM users before normal module unload.\n\nNever removes a PCI device or unbinds a controller. Never forces an unload.\n"""\nimport datetime\nimport fcntl\nimport json\nimport os\nfrom pathlib import Path\nimport re\nimport stat\nimport subprocess\nimport sys\nimport time\n\nMODULES=(\'nvidia_drm\',\'nvidia_modeset\',\'nvidia_uvm\',\'nvidia\')\nBASE=Path(\'/var/lib/gpd-egpu-eject\')\n\ndef check(ok,message):\n    if not ok:raise RuntimeError(message)\n\ndef valid_config(data):\n    check(len(data)>=16,\'PCI config inaccessible or short\')\n    check(not all(x==255 for x in data) and data[:2] not in (b\'\\xff\\xff\',b\'\\0\\0\') and data[14]&127!=127,\'Hardware/tunnel failure: PCI config invalid; no recovery\')\n\ndef pci(p):\n    with (p/\'config\').open(\'rb\',buffering=0) as f:valid_config(f.read(64))\n\ndef node_ids(nodes):\n    ids=set()\n    for p in nodes:\n        try:\n            s=p.stat()\n            if stat.S_ISCHR(s.st_mode):ids.add(s.st_rdev)\n        except FileNotFoundError:pass\n    return ids\n\ndef clients(ids):\n    result=[]\n    for proc in Path(\'/proc\').iterdir():\n        if not proc.name.isdecimal():continue\n        try:\n            for fd in (proc/\'fd\').iterdir():\n                try:\n                    s=fd.stat()\n                    if stat.S_ISCHR(s.st_mode) and s.st_rdev in ids:\n                        result.append({\'pid\':int(proc.name),\'name\':(proc/\'comm\').read_text().strip(),\n                                       \'uid\':proc.stat().st_uid,\'exe\':str((proc/\'exe\').resolve()),\n                                       \'device\':str(fd.resolve()),\'device_number\':[os.major(s.st_rdev),os.minor(s.st_rdev)]});break\n                except FileNotFoundError:pass\n        except FileNotFoundError:pass\n        except PermissionError:raise RuntimeError(\'Cannot inspect all GPU clients; refusing unload\')\n    return result\n\ndef session_holder(p):\n    expected={\'kwin_wayland\':(\'/usr/bin/kwin_wayland\',1000),\n              \'Xwayland\':(\'/usr/bin/Xwayland\',1000),\n              \'systemd-logind\':(\'/usr/lib/systemd/systemd-logind\',0),\n              \'systemd\':(\'/usr/lib/systemd/systemd\',0)}\n    return (p.get(\'exe\'),p.get(\'uid\'))==expected.get(p[\'name\'],(\'never\',-1)) and (p[\'name\']!=\'systemd\' or p[\'pid\']==1)\n\ndef classify_clients(found,allow_kwin=False):\n    check(not [p for p in found if not allow_kwin or not session_holder(p)],\n          \'GPU still used by \'+json.dumps(found)+\'; close applications, keep cable connected\')\n\ndef supported_kernel(kernel):\n    return kernel in (\'7.2.4-arch1-2\',\'7.2.5-1-cachyos\')\n\ndef main():\n    check(os.geteuid()==0,\'This action needs its graphical administrator authorization\')\n    check(not sys.argv[1:],\'No arguments accepted\')\n    os.umask(0o077)\n    lock=open(\'/run/lock/gpd-egpu-loader.lock\',\'a\')\n    fcntl.flock(lock,fcntl.LOCK_EX|fcntl.LOCK_NB)\n    boot=Path(\'/proc/sys/kernel/random/boot_id\').read_text().strip()\n    root=BASE/boot\n    check(not root.exists(),\'An eject was already attempted this boot; review its record, no automatic retry\')\n    check(supported_kernel(os.uname().release),\'Unsupported kernel for controlled eject\')\n    check(\'GZ302EA\' in Path(\'/sys/class/dmi/id/product_name\').read_text(),\'Wrong machine\')\n    check(Path(\'/sys/class/dmi/id/bios_version\').read_text().strip()==\'GZ302EA.314\',\'BIOS changed\')\n    gpus=[p for p in Path(\'/sys/bus/pci/devices\').iterdir() if (p/\'vendor\').read_text().strip()==\'0x10de\' and (p/\'class\').read_text().strip().startswith(\'0x03\')]\n    check(len(gpus)==1,\'Exactly one connected NVIDIA display GPU required\')\n    gpu=gpus[0];parent=Path(\'/sys/bus/pci/devices\')/gpu.resolve().parent.name\n    check((gpu/\'device\').read_text().strip()==\'0x2684\',\'Wrong GPU\')\n    check((parent/\'vendor\').read_text().strip()==\'0x8086\' and (parent/\'device\').read_text().strip()==\'0x5786\',\'Wrong bridge\')\n    pci(parent);pci(gpu)\n    panels=[p for p in Path(\'/sys/class/drm\').glob(\'card*-eDP-*\') if (p/\'enabled\').read_text().strip()==\'enabled\' and (p.parent/p.name.split(\'-\')[0]/\'device/vendor\').read_text().strip()==\'0x1002\']\n    check(panels,\'Internal AMD panel must already be enabled\')\n    params=Path(\'/proc/driver/nvidia/params\').read_text()\n    for key in (\'DynamicPowerManagement\',\'EnableGpuFirmware\'):\n        check(re.findall(r\'^\'+key+r\':\\s*(\\S+)\\s*$\',params,re.M)==[\'0\'],\'Working core options changed\')\n    for name,value in [(\'nvidia\',\'329D97755EFA31D5314E8F0\'),(\'nvidia_uvm\',\'064C13389A454AA4AAF638E\'),(\'nvidia_modeset\',\'C86CA92D8E48780F19CB46E\'),(\'nvidia_drm\',\'E22EBDCD6BCC5889463CFBF\')]:\n        check(Path(\'/sys/module\',name,\'srcversion\').read_text().strip()==value,\'Loaded build differs: \'+name)\n    gate=Path(\'/etc/modprobe.d/zzz-gpd-egpu-controlled-load.conf\').read_text().splitlines()\n    for name in (*MODULES,\'nvidia_peermem\',\'nouveau\'):\n        check(\'install \'+name+\' /usr/bin/false\' in gate,\'Automatic module blocking missing\')\n    drm=[p for p in Path(\'/sys/class/drm\').iterdir() if re.fullmatch(r\'(card\\d+|renderD\\d+)\',p.name) and (p/\'device\').resolve()==gpu.resolve()]\n    check(any(p.name.startswith(\'card\') for p in drm) and any(p.name.startswith(\'render\') for p in drm),\'Missing GPU DRM nodes\')\n    nodes=[Path(\'/dev/dri\')/p.name for p in drm]+list(Path(\'/dev\').glob(\'nvidia*\'))+list(Path(\'/dev/nvidia-caps\').glob(\'*\'))\n    ids=node_ids(nodes)\n    check(ids,\'No NVIDIA device numbers found\')\n    found=clients(ids)\n    prelogs=Path(\'/var/log/egpu-autoconfig/eject-preflight\')\n    prelogs.mkdir(parents=True,exist_ok=True)\n    (prelogs/(str(time.time_ns())+\'.json\')).write_text(json.dumps({\'boot\':boot,\'clients\':found},indent=2))\n    lact_status=subprocess.run([\'/usr/bin/systemctl\',\'show\',\'lactd.service\',\'--property=MainPID\',\'--value\'],capture_output=True,text=True,timeout=10)\n    check(lact_status.returncode==0,\'Cannot inspect LACT service\')\n    lact_pid=int(lact_status.stdout.strip() or \'0\')\n    lact_clients=[p for p in found if p[\'pid\']==lact_pid and p.get(\'uid\')==0 and p.get(\'exe\')==\'/usr/bin/lact\']\n    classify_clients([p for p in found if p not in lact_clients],allow_kwin=True)\n    root.mkdir(parents=True,mode=0o700)\n    log=(root/\'operations.log\').open(\'a\',buffering=1)\n    record={\'boot\':boot,\'gpu\':gpu.name,\'parent\':parent.name,\'drm\':[p.name for p in drm],\'stage\':\'preflight-complete\'}\n    def note(text):\n        line=datetime.datetime.now(datetime.timezone.utc).isoformat()+\' \'+text\n        print(line,flush=True);log.write(line+\'\\n\');log.flush();os.fsync(log.fileno())\n    def checkpoint(stage):\n        record[\'stage\']=stage\n        with (root/\'state.json\').open(\'w\') as f:json.dump(record,f,indent=2);f.flush();os.fsync(f.fileno())\n        note(stage)\n    def run(args):\n        note(repr(args));r=subprocess.run(args,capture_output=True,text=True,timeout=20)\n        note(r.stdout+r.stderr);check(r.returncode==0,\'Command failed; keep cable connected\')\n    lact_paused=False\n    unloaded=False\n    try:\n        if lact_clients:\n            checkpoint(\'before-temporary-lact-stop\')\n            run([\'/usr/bin/systemctl\',\'stop\',\'lactd.service\'])\n            lact_paused=True;record[\'lact_paused\']=True\n            classify_clients(clients(ids),allow_kwin=True)\n        checkpoint(\'before-userspace-drm-release\')\n        config=Path(\'/home/EGPU_USER/.config/kwinoutputconfig.json\')\n        if config.exists():(root/\'kwinoutputconfig.before\').write_bytes(config.read_bytes())\n        # Synthetic notification only, scoped to this GPU\'s DRM nodes. This is NOT\n        # a write to PCI remove/reset/rescan; hardware stays connected and bound.\n        for p in drm:\n            pci(parent);pci(gpu)\n            note(\'NOTIFY DRM userspace removal: \'+str(p/\'uevent\'))\n            with (p/\'uevent\').open(\'w\') as f:f.write(\'remove\\n\')\n        end=time.monotonic()+15\n        while True:\n            found=clients(ids)\n            if not found:break\n            if time.monotonic()>=end:\n                record[\'remaining_clients\']=found\n                classify_clients(found)\n            time.sleep(.5)\n        checkpoint(\'all-gpu-clients-released\')\n        for name in MODULES:\n            pci(parent);pci(gpu)\n            classify_clients(clients(ids))\n            p=Path(\'/sys/module\')/name\n            check(not list((p/\'holders\').iterdir()) and (p/\'refcnt\').read_text().strip()==\'0\',\'Module still referenced: \'+name+\'; no force-unload\')\n            checkpoint(\'before-normal-unload-\'+name)\n            unloaded=True\n            run([\'/usr/bin/rmmod\',name])\n            check(not p.exists(),\'Module remained loaded: \'+name)\n        pci(parent);pci(gpu)\n        checkpoint(\'all-nvidia-modules-unloaded\')\n        note(\'READY TO UNPLUG: all NVIDIA modules unloaded normally; internal panel remains available. LACT remains paused until next reboot to avoid reopening the GPU before physical removal. This is not yet a proven reconnect cycle.\')\n    except Exception as e:\n        record[\'error\']=str(e);checkpoint(\'failed-do-not-unplug\')\n        note(\'DO NOT UNPLUG. No forced unload, PCI recovery or automatic retry. \'+str(e))\n        if lact_paused and not unloaded:\n            try:\n                pci(parent);pci(gpu)\n                run([\'/usr/bin/systemctl\',\'start\',\'lactd.service\'])\n                record[\'lact_paused\']=False;checkpoint(\'failed-lact-restored-do-not-unplug\')\n            except Exception as restore_error:\n                note(\'LACT restoration not completed; retain cable and reboot normally before further testing: \'+str(restore_error))\n        raise\n    finally:log.close()\n\nif __name__==\'__main__\':\n    try:main()\n    except Exception as e:print(\'NOT READY TO UNPLUG: \'+str(e),file=sys.stderr);raise SystemExit(1)\n'}, '/opt/gpd-egpu/auto/restart-clean-cycle.py': {'before': '53ae8ea39b90901ea8214ad9b06e4651c9f49916bcff752a5ea0a8641f77de7c', 'after': 'b09c0907c13d689a20db7d798c2fc2f4a36ba4d5f8e9b75bf43db90359709bbf', 'content': '#!/usr/bin/env python3\n"""One restart per recorded clean-eject cycle. No driver replacement."""\nimport datetime\nimport fcntl\nimport hashlib\nimport importlib.util\nimport json\nimport os\nfrom pathlib import Path\nimport subprocess\nimport sys\nimport time\n\nsys.dont_write_bytecode=True\nEXPECTED={\n \'/opt/gpd-egpu/615.71.09/loader.py\':\'82127de15b2905f760dd7dea46b7dc1c8313a8c4c9342b9b4ef4cd6f31bb77f1\',\n \'/opt/gpd-egpu/display-extension/display-start.py\':\'ce4b95f8fd6b55ae154be9b32f82afd64550adb6d12a89b7d7162061865eea04\',\n \'/usr/local/sbin/gpd-egpu-start\':\'a980026e599653b390bcea1782be008dd7de782e3173a75405f2e2a346976774\',\n \'/usr/local/libexec/gpd-egpu-eject\':\'5f6bfc8d267f6818a81c5834534164c069e33bb3c3156c6415316caba1984fde\',\n}\n\ndef check(value,message):\n    if not value:raise RuntimeError(message)\n\ndef validate_records(boot,eject,core,display,loaded):\n    check(not loaded,\'NVIDIA modules remain loaded; no unload/reload attempted\')\n    check(eject.get(\'boot\')==boot and eject.get(\'stage\')==\'all-nvidia-modules-unloaded\',\n          \'No completed clean eject for this boot; no retry\')\n    for label,record in [(\'core\',core),(\'display\',display)]:\n        check(record.get(\'boot_id\')==boot and record.get(\'stage\')==\'completed\',\n              label+\' loader did not previously complete this boot; no retry\')\n\ndef archive(source,dest):\n    check(source.is_dir() and not source.is_symlink(),\'Unexpected record directory: \'+str(source))\n    check(not dest.exists(),\'Archive destination already exists\')\n    source.rename(dest)\n    for p in (source.parent,dest.parent):\n        fd=os.open(p,os.O_DIRECTORY)\n        try:os.fsync(fd)\n        finally:os.close(fd)\n\ndef cycle_id(eject):\n    return hashlib.sha256((eject/\'state.json\').read_bytes()+b\'\\0\'+(eject/\'operations.log\').read_bytes()).hexdigest()\n\ndef validate_marker(automatic, marker, boot, cycle):\n    if automatic:\n        check(marker is not None and marker.get(\'boot\')==boot and marker.get(\'cycle\')==cycle and marker.get(\'action\')==\'clean-eject-restart\',\'Automatic invocation does not match its checkpoint\')\n    else:\n        check(marker is None,\'Automatic loader failure marker exists; no retry\')\n\ndef main():\n    check(os.geteuid()==0 and sys.argv[1:] in ([],[\'--automatic\']),\'Root execution required; invalid arguments\')\n    automatic=sys.argv[1:]==[\'--automatic\']\n    os.umask(0o077)\n    boot=Path(\'/proc/sys/kernel/random/boot_id\').read_text().strip()\n    eject_source=Path(\'/var/lib/gpd-egpu-eject\')/boot\n    cycle=cycle_id(eject_source)\n    base=Path(\'/var/lib/gpd-egpu-restart\')/boot/cycle\n    check(not base.exists(),\'Restart already attempted for this eject cycle; inspect its log, no automatic retry\')\n    lock=open(\'/run/lock/gpd-egpu-loader.lock\',\'a\')\n    fcntl.flock(lock,fcntl.LOCK_EX|fcntl.LOCK_NB)\n    for name,digest in EXPECTED.items():\n        check(hashlib.sha256(Path(name).read_bytes()).hexdigest()==digest,\'Working driver helper changed: \'+name)\n    spec=importlib.util.spec_from_file_location(\'working_loader\',next(iter(EXPECTED)))\n    l=importlib.util.module_from_spec(spec);spec.loader.exec_module(l)\n    m=l.guard();m.LOGDIR=Path(\'/var/log/egpu-autoconfig/restart-after-eject\')/boot\n    t=m.Trial()\n    try:\n        t.log(\'SCRIPT SHA256 \'+hashlib.sha256(Path(__file__).read_bytes()).hexdigest())\n        check(os.uname().release==\'7.2.4-arch1-2\',\'Exact tested Arch kernel required\')\n        core=Path(\'/var/lib/gpd-egpu-loader\')/boot\n        display=Path(\'/var/lib/gpd-egpu-display\')/boot\n        eject=Path(\'/var/lib/gpd-egpu-eject\')/boot\n        records=[json.loads((p/f).read_text()) for p,f in [(eject,\'state.json\'),(core,\'attempt.json\'),(display,\'attempt.json\')]]\n        validate_records(boot,*records,any(Path(\'/sys/module\',n).exists() for n in (\'nvidia\',\'nvidia_uvm\',\'nvidia_modeset\',\'nvidia_drm\',\'nouveau\',\'nvidia_peermem\')))\n        l.identity(t,m)\n        marker=Path(\'/var/lib/gpd-egpu-auto/initializing-or-failed.json\')\n        validate_marker(automatic,json.loads(marker.read_text()) if marker.exists() else None,boot,cycle)\n        check((Path(\'/var/lib/gpd-egpu-auto\')/(boot+\'.completed\')).is_file(),\n              \'Automatic first-activation completion record missing; review concurrency before restarting\')\n        check(t.run([\'systemctl\',\'is-active\',\'gpd-egpu-auto.service\'],check=False).strip()in ((\'activating\',) if automatic else (\'inactive\',)),\'Unexpected automatic loader state\')\n        check(not Path(\'/run/gpd-egpu-display-trial.active\').exists(),\'Experimental candidate trial remains active\')\n        gpu,parent,hda=l.find_gpu(t,m,False)\n        check(not (gpu/\'driver\').exists(),\'GPU is already bound; no takeover\')\n        for p in (parent,gpu,*hda):t.pci(p.name)\n        panels=[p for p in Path(\'/sys/class/drm\').glob(\'card*-eDP-*\')\n                if (p/\'enabled\').read_text().strip()==\'enabled\'\n                and (p.parent/p.name.split(\'-\')[0]/\'device/vendor\').read_text().strip()==\'0x1002\']\n        check(panels,\'Internal AMD panel must be enabled\')\n        base.mkdir(parents=True,mode=0o700)\n        record={\'boot\':boot,\'stage\':\'prepared\',\'gpu\':gpu.name,\'parent\':parent.name,\n                \'undo\':\'No driver/configuration/package/boot changes. Normal reboot clears restarted modules and uses normal automatic loading. Original and new per-boot histories are retained.\',\n                \'source_hashes\':EXPECTED,\'archived\':[]}\n        def checkpoint(stage):\n            record[\'stage\']=stage\n            tmp=base/\'state.json.tmp\'\n            with tmp.open(\'w\') as f:json.dump(record,f,indent=2);f.flush();os.fsync(f.fileno())\n            tmp.replace(base/\'state.json\');os.sync();t.log(\'CHECKPOINT \'+stage)\n        (base/\'runner.py\').write_bytes(Path(__file__).read_bytes())\n        config=Path(\'/home/EGPU_USER/.config/kwinoutputconfig.json\')\n        if config.exists():(base/\'kwinoutputconfig.before\').write_bytes(config.read_bytes())\n        checkpoint(\'before-archive\')\n        for label,p in [(\'core\',core),(\'display\',display)]:\n            record[\'archive_intent\']=[str(p),str(base/(label+\'-before\'))];checkpoint(\'archiving-\'+label)\n            archive(p,base/(label+\'-before\'));record[\'archived\'].append(label)\n        checkpoint(\'before-working-loader\')\n        # The unchanged auto completion marker prevents udev from restarting it.\n        # Release the shared lock for the existing loader, which acquires it.\n        fcntl.flock(lock,fcntl.LOCK_UN)\n        try:\n            t.run([\'/usr/local/sbin/gpd-egpu-start\'],timeout=540)\n        except BaseException:\n            checkpoint(\'failed-no-retry\');raise\n        fcntl.flock(lock,fcntl.LOCK_EX|fcntl.LOCK_NB)\n        l.live(t,m)\n        for module,expected in [(\'nvidia_modeset\',\'C86CA92D8E48780F19CB46E\'),(\'nvidia_drm\',\'E22EBDCD6BCC5889463CFBF\')]:\n            check(t.read(\'/sys/module/\'+module+\'/srcversion\')==expected,\'Unexpected restarted display module\')\n        gpu,parent,hda=l.find_gpu(t,m,False);t.check_link(gpu,parent,hda);l.nvml(t,m)\n        for p in (core,display):check(json.loads((p/\'attempt.json\').read_text())[\'stage\']==\'completed\',\'New loader record incomplete\')\n        check(json.loads((eject/\'state.json\').read_text())==records[0],\'Eject record changed concurrently\')\n        checkpoint(\'before-retiring-completed-eject\')\n        archive(eject,base/\'eject-before\')\n        checkpoint(\'completed\')\n        t.log(\'READY: original NVIDIA compute/display drivers restarted after clean eject. Disconnect app is available again. Keep cable connected until the app reports READY TO UNPLUG. Surprise unplug remains unsafe. LACT state unchanged.\')\n    except (Exception,KeyboardInterrupt) as e:\n        t.log(\'FAIL: \'+str(e)+\'; no unload, recovery, record deletion or retry attempted. Evidence: \'+str(base))\n        raise SystemExit(1)\n    finally:t.stop_follow()\n\nif __name__==\'__main__\':main()\n'}}

def check(ok,msg):
 if not ok:raise RuntimeError(msg)
def sha(p):return hashlib.sha256(p.read_bytes()).hexdigest()
def put(p,data,mode):
 temp=p.with_name(p.name+'.cachyos-eject-tmp')
 with temp.open('xb') as f:f.write(data);f.flush();os.fsync(f.fileno())
 temp.chmod(mode);temp.replace(p)
def undo():
 records=json.loads((BACK/'changes.json').read_text())
 for name,r in records.items():
  p=Path(name);check(not p.is_symlink() and sha(p) in (r['before'],r['after']),'Changed file; refuse rollback: '+name)
  check(sha(BACK/r['backup'])==r['before'],'Backup corrupted')
 for name,r in records.items():put(Path(name),(BACK/r['backup']).read_bytes(),r['mode'])
 os.sync();print('Eject support change undone; no module, boot, service or power operations. Diagnostics retained.')
def main():
 check(os.geteuid()==0 and sys.argv[1:] in ([],['--undo']),'Run with sudo; only optional --undo accepted');os.umask(0o077)
 lock=open('/run/lock/gpd-egpu-loader.lock','a');fcntl.flock(lock,fcntl.LOCK_EX|fcntl.LOCK_NB)
 check(subprocess.run(['systemctl','is-active','gpd-egpu-auto.service'],capture_output=True,text=True).stdout.strip()=='inactive','Auto loader must be idle')
 if sys.argv[1:]:undo();return
 check(not BACK.exists(),'Support change already staged; preserve prior records')
 check(os.uname().release=='7.2.5-1-cachyos','Current tested CachyOS kernel required')
 record=json.loads(Path('/root/egpu-cachyos-7.2.5-validation/attempt.json').read_text())
 check(record.get('stage')=='completed' and record.get('result','').startswith('PASS:') and record.get('boot_id')==Path('/proc/sys/kernel/random/boot_id').read_text().strip(),'Completed trial in this boot required')
 changes={}
 for name,r in FILES.items():
  p=Path(name);check(not p.is_symlink() and sha(p)==r['before'],'Installed helper differs: '+name)
  data=r['content'].encode();check(hashlib.sha256(data).hexdigest()==r['after'],'Payload mismatch');compile(data,name,'exec');changes[p]=data
 metadata=Path('/root/egpu-clean-reconnect-backup/expected.json');v=json.loads(metadata.read_text())
 r=FILES['/opt/gpd-egpu/auto/restart-clean-cycle.py'];check(v['helper']==r['before'],'Reconnect rollback hash changed');v['helper']=r['after'];changes[metadata]=(json.dumps(v,indent=2)+'\n').encode()
 BACK.mkdir(mode=0o700);records={}
 for i,(p,data) in enumerate(changes.items()):
  saved=str(i)+'.before';(BACK/saved).write_bytes(p.read_bytes())
  records[str(p)]={'before':sha(p),'after':hashlib.sha256(data).hexdigest(),'backup':saved,'mode':stat.S_IMODE(p.stat().st_mode)}
 (BACK/'changes.json').write_text(json.dumps(records,indent=2)+'\n');(BACK/'installer.py').write_bytes(Path(__file__).read_bytes())
 (BACK/'rollback.sh').write_text('#!/bin/bash\nset -euo pipefail\nexec /usr/bin/python3 /root/gpd-egpu-cachyos-7.2.5-eject-backup/installer.py --undo\n')
 (BACK/'rollback.sh').chmod(0o700);subprocess.run(['bash','-n',str(BACK/'rollback.sh')],check=True);os.sync()
 try:
  for p,data in changes.items():put(p,data,records[str(p)]['mode'])
  for p in changes:check(sha(p)==records[str(p)]['after'],'Post-write hash mismatch')
 except BaseException:
  for p in changes:put(p,(BACK/records[str(p)]['backup']).read_bytes(),records[str(p)]['mode'])
  raise
 os.sync();print('INSTALLED: tray-controlled eject supports Arch 7.2.4 and CachyOS 7.2.5. No driver or service action performed. CachyOS reconnect/automatic loading still not enabled. Use tray Disconnect safely; unplug only after READY TO UNPLUG. Undo this layer before earlier rollbacks: '+str(BACK/'rollback.sh'))
if __name__=='__main__':
 try:main()
 except Exception as e:raise SystemExit('STOP: '+str(e)+'; no automatic driver recovery or reboot')
